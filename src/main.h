#ifndef NUMBER_AMOUNT
#define NUMBER_AMOUNT 60
#endif

#ifndef MAIN_H
#define MAIN_H
void clearLines(int lines);
void mapNumbersToBuffer(char *buffer, int *numbers);
void clearBuffer(char *buffer);
void printBuffer(char *buffer);
#endif
